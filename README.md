# Ansible base playbooks

for configuring a ubuntu dist on a Thinkpad E490

## Base packages

net-tools includes: arp, hostname, ifconfig, netstat, rarp, route, plipconfig, slattach, mii-tool and iptunnel and ipmaddr.